---
title: Bank connections
description: Link a bank to Freenary, watch the import, re-sync it, and live with a connection that breaks.
icon: Landmark
---

A bank connection is the link between Freenary and one bank. Every account, balance and transaction in Freenary arrives through one. This page covers the whole life of a connection.

## Where to start

The same panel sits on two screens:

- The `Connect your bank` step of the setup. See [First steps](/docs/guides/first-steps).
- Settings, group `Connections`, section `Bank accounts`.

<Callout type="warn">

Your instance links banks through one service, and your operator sets it up. Without one, the section reads `Bank linking is unavailable` over `This instance has no banking provider configured, so there is nothing to link against yet.` Ask your operator, and send them [Bank providers](/docs/self-hosting/bank-providers).

</Callout>

A check that fails reads `Could not check bank linking`, over the line `Reload the page to try again`. That is not the same answer. Reload the page before you ask anybody.

## The institution list

The panel holds one row per bank, under the search box `Search banks…`. The list has its own scroll bar.

Your connected banks sit at the top. Each of those rows carries a sub-line:

| Sub-line                      | What it means                             |
| ----------------------------- | ----------------------------------------- |
| `{accounts} · Synced {date}`  | The date of the last sync that worked     |
| `{accounts} · Not synced yet` | The connection exists and no sync has run |

A bank you already connected drops out of the list of banks to connect. You cannot link the same bank twice from here. Every other row carries a `Connect` button.

## Link a bank

<Steps>

<Step>

### Press Connect

Freenary asks the service for a one-time link, then sends your browser to it. Freenary links nothing at this point.

</Step>

<Step>

### Answer the bank page

The page belongs to your bank, or to the service that links it. It never belongs to Freenary. Sign in there, pass the strong authentication your bank asks for, and pick the accounts to share.

</Step>

<Step>

### Come back

The bank sends your browser to a Freenary page. A screen reader hears `Finishing bank connection` while it works. Freenary checks the answer, creates the connection, and records the accounts the bank returned.

You read `Bank connected`, then `Connected {count} account(s)`. Freenary drops you back on Settings, or on the setup when you started there.

</Step>

</Steps>

## What arrives

| Freenary imports | Detail |
| --- | --- |
| Bank accounts | The accounts you shared, with the name and the account number the bank reports |
| The balance of an account | Recorded when the service reports it. No screen shows a balance |
| Transactions | Every transaction inside the sync window, filed under a category |
| Investment holdings | Only on an instance whose service returns them |

<Callout type="info">

Freenary can link banks through one of two services, and only one of the two returns your investment holdings. Ask your operator which one your instance uses. See [Bank providers](/docs/self-hosting/bank-providers).

</Callout>

## Sync

Nothing refreshes your data at night. A sync runs in one of three cases:

| Trigger | What runs |
| --- | --- |
| You open Budget and you hold bank accounts | One quiet sync, once per visit. It reports a failure and nothing else |
| You press `Sync transactions` on Budget | A full re-read for every connection |
| You press the re-sync button on one row of the bank list | A full re-read for that connection alone |

**Every sync reads a window of 90 days at most.** A first sync reads those 90 days. A later sync starts at the date of the last sync that worked, so it reads less. A full re-read covers the whole 90 days again. It files every transaction in that window under a category again.

Budget reports the result: `Transactions are up to date`, or `Up to date · {count} transaction(s) categorised` when it filed some. A failure reads `Failed to sync transactions` and offers `Retry`. A single row reports `{institution} re-synced`.

<Callout type="warn">

A transaction older than 90 days never arrives. No control widens the window.

</Callout>

## Disconnect a bank

Press `Disconnect` on a connected row. The dialog reads `Disconnect {institution}?` and counts what goes. Confirm with `Disconnect bank`, or leave with `Cancel`.

On confirmation Freenary does two things, in this order:

1. It asks your bank to revoke the access of Freenary.
2. It deletes the connection, its bank accounts, their transactions and their holdings.

You read `{institution} disconnected`, or `{institution} disconnected — {count} account(s) removed`.

A revoke that fails stops nothing. Freenary still deletes the data and warns you: `{institution} disconnected, but freenary could not ask your bank to revoke access. Revoke it from your bank to be sure.` Sign in at your bank and revoke it there.

A disconnect keeps your budgeting profile, your custom categories, and the category corrections you made by hand.

## When a connection breaks

A bank consent runs out, or your bank asks for a fresh strong authentication. Freenary then fails the sync for that bank and reads `Failed to sync transactions`. The row itself does not change, and the date of the last sync stops moving.

Freenary offers no reconnect button. Two actions sit on a connected row: re-sync and disconnect.

<Steps>

<Step>

### Try a re-sync first

Press the re-sync button on the row. A transient failure clears itself here.

</Step>

<Step>

### Disconnect the bank

The old connection carries the consent your bank refused. Disconnect it, and accept that its accounts and transactions go with it.

</Step>

<Step>

### Connect the bank again

The bank comes back to the list of banks to connect. Press `Connect` and answer the bank page again. This writes a **new** connection, so the import starts at the top of the 90-day window.

</Step>

</Steps>

## Failure table

| What happened | What you read | What to do |
| --- | --- | --- |
| Your instance links no bank | `Bank linking is unavailable` | Ask your operator |
| The check for that itself failed | `Could not check bank linking` | Reload the page |
| The bank list did not load | `Could not load banks` | Reload the page |
| Your connections did not load | `Could not load your banks` | Reload the page |
| The search matched nothing | `No banks match your search` | Search for a shorter part of the name |
| The service lists no bank | `No banks available` | Ask your operator |
| You cancelled at the bank | `Bank connection cancelled. Nothing was linked.` | Press `Connect` again |
| The bank answered in part | `The bank sent back an incomplete response.` | Press `Connect` again |
| The exchange failed | `Bank connection failed. You can try again.` | Press `Connect` again |
| A sync failed | `Failed to sync transactions` | Press `Retry`. Other connections still synced |

<Cards>
  <Card
    title="Budget"
    href="/docs/guides/budget"
    description="Read the transactions the import brought in."
  />
  <Card
    title="Categories and budget lines"
    href="/docs/guides/categories"
    description="Shape how the imported transactions are grouped."
  />
  <Card
    title="Settings"
    href="/docs/guides/settings"
    description="The screen that holds the bank list."
  />
</Cards>
